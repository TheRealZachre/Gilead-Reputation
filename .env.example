AUTH_SECRET=
AUTH_ADMIN_EMAILS=admin@beone.com
AUTH_URL=https://beonereputation.vibecodeflow.com

# Password reset emails (optional).
# Without these, the "Forgot password" flow shows the secure reset link
# on screen instead of emailing it. Set both to send real emails via Resend.
RESEND_API_KEY=
AUTH_EMAIL_FROM=
